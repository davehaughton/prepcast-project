{
  "name": "prepcast",
  "version": "0.1.0",
  "private": true,
  "description": "PrepCast — demand forecast dashboard",
  "scripts": {
    "dev": "tailwindcss -i ./src/input.css -o ./static/dist/output.css --watch",
    "build": "tailwindcss -i ./src/input.css -o ./static/dist/output.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.1",
    "tailwindcss": "^4.3.1"
  },
  "dependencies": {
    "@tailwindcss/oxide-darwin-arm64": "^4.3.2"
  }
}
